Script(
  BlockConditional(
    Name,
    ContentLine,
    BlockConditionCase(Name),
    ContentLine
  ),
  ContentLine,
  BlockConditional(
    BlockConditionCase(ExpressionComparison(ExpressionMod(Int,Int),Int)),
    ContentLine(Divert(DivertArrow,DivertTarget(Path))),
    BlockConditionCase(Name),
    ContentLine
  )
)