Script(
  Knot(
    KnotName,
    ContentLine,
    VariableAssignment(Name,Adjust(List(ListMember,ListMember))),
    ContentLine(Divert(TunnelReturn))
  ),
  Knot(
    KnotName,
    ContentLine,
    ContentLine(Divert(TunnelReturn))
  )
)